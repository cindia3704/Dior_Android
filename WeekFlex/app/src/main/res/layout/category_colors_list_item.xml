<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginRight="36dp">
    <ImageView
        android:id="@+id/category_color"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:src="@drawable/color_circle"/>
    <ImageView
        android:id="@+id/category_color_check_img"
        android:layout_width="24dp"
        android:layout_height="24dp"
        app:layout_constraintTop_toTopOf="@id/category_color"
        app:layout_constraintBottom_toBottomOf="@id/category_color"
        app:layout_constraintLeft_toLeftOf="@id/category_color"
        app:layout_constraintRight_toRightOf="@id/category_color"
        android:src="@drawable/white_check"
        android:visibility="visible"/>

</androidx.constraintlayout.widget.ConstraintLayout>